# assistant-app

Monorepo: Spring Boot (`backend`) + React / Vite (`frontend`).

## Backend

```bash
cd backend
./mvnw spring-boot:run
```

## Frontend

```bash
cd frontend
npm install
npm run dev
```

Vite: http://localhost:5173
